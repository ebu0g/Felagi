import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF064420);
  static const secondary = Color(0xFF0B6B3A);
  static const accent = Color(0xFF89E4A2);
  static const background = Colors.white;
}
